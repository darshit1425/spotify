import 'package:flutter/material.dart';

class Premimum extends StatefulWidget {
  const Premimum({Key? key}) : super(key: key);

  @override
  State<Premimum> createState() => _PremimumState();
}

class _PremimumState extends State<Premimum> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(),
    );
  }
}
